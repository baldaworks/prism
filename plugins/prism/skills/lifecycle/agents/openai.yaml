interface:
  display_name: "Prism Lifecycle"
  short_description: "Full role-aligned Prism lifecycle executed in the host"
  default_prompt: "Use $prism:lifecycle to run the full role-aligned Prism lifecycle directly in this host session."
